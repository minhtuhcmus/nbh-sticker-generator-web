export interface IItemDetail {
  name: string;
  packaging?: string;
  origin: string;
  provider: string;
}

export interface IPrintDetail {
  name: string;
  packaging?: string;
  origin: string;
  provider: string;
  quantity: number;
}